const Cards = ({courses}) => {
    //all courses data in one array
    const getAllCourses =()=>{
        Object.values(courses).forEach( );
    }
    return (
        <div>
            courses.map
        </div>
    );
}
export default Cards;